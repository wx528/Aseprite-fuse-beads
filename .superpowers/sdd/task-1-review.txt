3b27abd Add headless test harness
 run-tests.ps1        |  3 +++
 tests/run.lua        | 38 ++++++++++++++++++++++++++++++++++++++
 tests/test_smoke.lua |  1 +
 3 files changed, 42 insertions(+)
diff --git a/run-tests.ps1 b/run-tests.ps1
new file mode 100644
index 0000000..e022354
--- /dev/null
+++ b/run-tests.ps1
@@ -0,0 +1,3 @@
+$out = & "D:\SteamLibrary\steamapps\common\Aseprite\Aseprite.exe" -b --script "$PSScriptRoot\tests\run.lua" 2>&1 | Out-String
+$out
+if ($out -match "ALL TESTS PASSED") { exit 0 } else { exit 1 }
diff --git a/tests/run.lua b/tests/run.lua
new file mode 100644
index 0000000..2c0abf5
--- /dev/null
+++ b/tests/run.lua
@@ -0,0 +1,38 @@
+local src = debug.getinfo(1, "S").source:sub(2)
+local dir = src:match("^(.*)[/\\]")
+
+local passed, failed = 0, 0
+local failures = {}
+
+function ok(cond, msg)
+  if cond then
+    passed = passed + 1
+  else
+    failed = failed + 1
+    failures[#failures + 1] = "FAIL: " .. (msg or "assertion")
+  end
+end
+
+function eq(got, want, msg)
+  ok(got == want, (msg or "") .. " [want=" .. tostring(want) .. " got=" .. tostring(got) .. "]")
+end
+
+local files = {
+  "test_smoke.lua",
+}
+
+for _, f in ipairs(files) do
+  local chunk, err = pcall(dofile, dir .. "/" .. f)
+  if not chunk then
+    failed = failed + 1
+    failures[#failures + 1] = "ERROR in " .. f .. ": " .. tostring(err)
+  end
+end
+
+print(string.format("passed=%d failed=%d", passed, failed))
+for _, msg in ipairs(failures) do print(msg) end
+if failed == 0 then
+  print("ALL TESTS PASSED")
+else
+  print("TESTS FAILED")
+end
diff --git a/tests/test_smoke.lua b/tests/test_smoke.lua
new file mode 100644
index 0000000..b4a8cac
--- /dev/null
+++ b/tests/test_smoke.lua
@@ -0,0 +1 @@
+eq(1 + 1, 2, "smoke")
