# Changelog

## [1.0.0] - 2026-09-25

### 功能

- 导出当前 sprite 当前帧（合并图层）为拼豆图纸 PNG：网格 + 豆子 + 每格色号标注（3x5 位图字体，黑/白自动对比色）
- MARD/漫漫小豆 221 实色色板（A–H + M 系列），RGB 取自官方色卡
- 颜色匹配：RGB → Lab，CIEDE2000 最近邻；色板色完全一致时直接命中
- 豆子形状可选方形（默认，填满格子）/ 圆形（直径可调 50%–100%）
- 分格线：每 N 格深色加粗线（默认 5，0/1 关闭），四周边框始终加粗
- 用量统计区：按色号自然排序（字母 + 数字），每列 5 个，色块 + 色号 + 颗数
- 透明像素留空
- 单边超过 128px 时弹出卡死风险警告，可取消或执意导出
- 无 sprite / 保存失败等错误有中文提示

### 修复

- 统计区色号标签在窄图纸上被裁掉（输出宽度未计入标签宽度）
- 统计标签分隔符 "x" 无字形导致渲染成空白间隔（改为 "X"）
- 格子大小输入框清空后导出报错的指引不友好（默认值兜底）
- 色号文字过大盖住整颗豆子（限制为格子的 60% 宽 / 40% 高）
- 含透明像素的图纸统计区为空（matches 稀疏表按 pairs 遍历）
