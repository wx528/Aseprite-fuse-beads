# Aseprite 拼豆图纸导出插件 �?设计文档

日期�?026-09-24
状态：已确�?
## 目标

做一�?Aseprite 插件，把当前 sprite 导出为拼豆（fuse beads）图�?PNG：每格一颗圆�?+ 色号标注，自动匹�?MARD/漫漫 色号体系，并附用量统计�?
## 形�?
- 第一阶段：单�?Lua 脚本 `fuse-beads-export.lua`，放�?Aseprite 脚本目录，通过 文件 > 脚本 运行�?- 第二阶段：脚本稳定后打包�?`.aseprite-extension`（含 `package.json`），可双击安装、带菜单入口�?
## 功能需�?
1. 运行时弹出对话框，选项�?   - 色板选择（第一阶段�?MARD，结构上预留多品牌）
   - 格子大小 cellSize（默�?32px�?   - 输出路径（默认：`原文件名_pattern.png`，与原图同目录）
   - 是否包含用量统计区（默认开�?   - 圆豆直径占格子比例（默认�?90%，留出网格空隙）
2. 读取当前帧合并（flatten）后的图像，像素 1:1 对应豆子，不缩放�?3. 每个像素颜色映射到最近的 MARD 色号�?   - 与色板颜色完全一�?�?直接命中
   - 否则 RGB �?Lab，按 CIE76 取最近色�?4. 渲染 PNG�?   - 浅灰网格线分�?   - 每格画纯色圆豆（无高光，第一阶段从简�?   - 圆豆中心标注色号数字�?x5 位图字体，黑/白按底色亮度自动选对比色
   - 色号超过 2 位时自动使用缩小字号，保�?3 位以内可�?5. 透明像素：格子留空，不画圆豆不标色号，网格线照画�?6. 图纸下方绘制用量统计区：每行 = 色块 + 色号 + 颗数，按颗数降序�?7. 保存 PNG 后弹出完成提示�?
## 错误处理

- 无打开�?sprite �?报错对话框并中止
- 输出路径不可�?/ 保存失败 �?捕获异常并提�?
## 结构（单文件内分 section�?
- `MARD_PALETTE`：色号表（色号字符串、名称、RGB），先内置常用色，后续扩�?- `color.lua` 段：RGB↔Lab 转换、最近色号匹�?- `font.lua` 段：3x5 位图数字字体及缩放绘�?- `render.lua` 段：网格、圆豆、色号、统计区绘制
- `dialog.lua` 段：参数对话�?- `main()`：入口与流程串联

## 测试

无自动化测试框架，手动验收：

- 准备测试图：纯色、渐变、含透明像素、含色板外颜�?- 跑脚本目检输出 PNG
- 控制台输出匹配结果表，核对色号正确�?
## 非目标（YAGNI�?
- 多帧/多图层分别导�?- 图像缩放、旋�?- 圆豆高光/立体效果
- PDF、CSV 等其他输出格�?